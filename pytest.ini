[pytest]
filterwarnings =
    # scipy internals use np.find_common_type which is deprecated in NumPy >=1.25;
    # not actionable from our code — suppress until scipy ships a fix.
    ignore::DeprecationWarning:scipy
